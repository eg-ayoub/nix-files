from libqtile.backend.wayland.inputs import InputConfig

# When using the Wayland backend, this can be used to configure input devices.
# set keyboard layout between fr and us and swap using super+space
wl_input_rules = {
        # "type:keyboard": InputConfig(kb_layout="us,fr", kb_options="grp:win_space_toggle")
}
